---
- name: Install Docker using geerlingguy.docker role
  hosts: all
  become: true

  roles:
    - geerlingguy.docker

  # Optional: customize Docker here
  # vars:
  #   docker_edition: 'ce'
  #   docker_install_compose: true
  #   docker_users:
  #     - SSH_USER